operator:
  image:
    repository: ghcr.io/kape-io/kape-operator
    tag: "0.1.0"
    pullPolicy: IfNotPresent
  replicaCount: 1
  leaderElect: true

taskService:
  image:
    repository: ghcr.io/kape-io/kape-task-service
    tag: "0.1.0"
    pullPolicy: IfNotPresent
  replicaCount: 1
  database:
    secretName: kape-task-service-db

runtime:
  image:
    repository: ghcr.io/kape-io/kape-runtime
    tag: "0.1.0"
    pullPolicy: IfNotPresent

dashboard:
  image:
    repository: ghcr.io/kape-io/kape-dashboard
    tag: "0.1.0"
    pullPolicy: IfNotPresent
  replicaCount: 1
  traceUrlTemplate: ""
  ingress:
    enabled: false
    host: ""

adapters:
  falco:
    enabled: true
    image:
      repository: ghcr.io/kape-io/kape-adapter-falco
      tag: "0.1.0"
  alertmanager:
    enabled: false
    image:
      repository: ghcr.io/kape-io/kape-adapter-alertmanager
      tag: "0.1.0"
  audit:
    enabled: false
    image:
      repository: ghcr.io/kape-io/kape-adapter-audit
      tag: "0.1.0"

# kapeproxy sidecar image used by operator-managed handler pods.
# The release pin lives here (per D17), not in operator Go code or in
# playground/Tilt dev tooling (which use :local per D18).
kapeproxy:
  image:
    repository: kape/kapeproxy
    tag: "0.7.0"
    pullPolicy: IfNotPresent

nats:
  replicaCount: 3
  storage: 10Gi
  image:
    repository: nats
    tag: "2.10-alpine"
    pullPolicy: IfNotPresent
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
  persistence:
    retain: false

postgres:
  instances: 2
  storage: 10Gi
  image:
    repository: ghcr.io/cloudnative-pg/postgresql
    tag: "16.3"
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 1Gi

certManager:
  enabled: true
  issuerEmail: ""
  clusterIssuer:
    staging: true
    production: false

namespace: kape-system
clusterName: ""

oauth2Proxy:
  enabled: false
  image:
    repository: quay.io/oauth2-proxy/oauth2-proxy
    tag: "v7.6.0"
    pullPolicy: IfNotPresent
  provider: google
  emailDomain: "*"
  secretName: kape-oauth2-proxy
  ingress:
    host: ""

# crds controls the pre-install hook that applies the kape.io CRDs and the
# KapeHandler validating webhook configuration via a ConfigMap-backed Job.
crds:
  install: true
