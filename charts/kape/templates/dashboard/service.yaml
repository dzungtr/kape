apiVersion: v1
kind: Service
metadata:
  name: kape-dashboard
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: kape-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 3000
