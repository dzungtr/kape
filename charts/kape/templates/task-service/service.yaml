apiVersion: v1
kind: Service
metadata:
  name: kape-task-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: kape-task-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
